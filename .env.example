# Safe default: external engines are not executed.
DRY_RUN=true
XBOW_PORT=8080
WORKER_TIMEOUT_SECONDS=7200

# Configure provider secrets only on the server, never commit them.
# LLM_API_KEY=
# STRIX_LLM=
